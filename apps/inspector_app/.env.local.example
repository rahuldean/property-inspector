# URL to the Go property-inspector API (local docker-compose default: http://localhost:6000)
INSPECTOR_API_URL=http://localhost:6000

# Optional: set a token to protect the app. Leave empty to disable auth in dev.
# APP_TOKEN=your-secret-token
